tool: Gitleaks
slug: gitleaks
language: javascript
category: security
technique: "Secret Scanning"
classification: "Hardcoded Secrets"
target_files: [../../security/fixtures/secret-scan-fixture.txt]
run:
  command: "gitleaks detect --source . --report-format json --report-path gitleaks.json"
expects:
  secret_report_generated: true
verified_in_session: true
verified_result: "ran 2026-08-18 against security/fixtures/secret-scan-fixture.txt with gitleaks v8.30.1: scanned 203 bytes, 0 leaks found (fixture uses deliberately non-matching synthetic values)"
