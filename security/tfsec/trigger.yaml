tool: tfsec
slug: tfsec
language: javascript
category: security
technique: "IaC Static Analysis"
classification: "Infra Misconfiguration"
target_files: [../../security/fixtures/main.tf]
run:
  command: "tfsec security/fixtures --format json"
expects:
  iac_report_generated: true
verified_in_session: false
verified_result: "valid Terraform fixture supplied; requires tfsec"
