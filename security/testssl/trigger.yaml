tool: testssl.sh
slug: testssl
language: javascript
category: security
technique: "TLS/SSL Configuration Scan"
classification: "Transport Security Compliance"
target_files: [../../security/fixtures/openapi.yaml]
run:
  command: "testssl.sh --jsonfile testssl.json ${TLS_TARGET:?set TLS_TARGET}"
expects:
  tls_report_generated: true
verified_in_session: false
verified_result: "requires reachable TLS_TARGET and testssl.sh"
