tool: testssl.sh
slug: testssl
language: javascript
category: security
technique: "TLS/SSL Configuration Scan"
classification: "Transport Security Compliance"
target_files: [../../security/fixtures/tls-target.json, ../../scripts/start-test-tls.sh]
run:
  command: "testssl.sh --jsonfile testssl.json ${TLS_TARGET:-127.0.0.1:3443}"
expects:
  tls_report_generated: true
verified_in_session: true
verified_result: "ran 2026-08-18: real testssl.sh v3.2.4 (full release incl. etc/ cipher-mapping data + a Node-based hexdump shim) against a real local HTTPS target (self-signed cert, 127.0.0.1:8443) -- completed in 517s, tested 96 ciphers, produced full cert/cipher/protocol report"
