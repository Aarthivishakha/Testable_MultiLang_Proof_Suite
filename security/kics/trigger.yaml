tool: KICS
slug: kics
language: javascript
category: security
technique: "IaC Static Analysis"
classification: "Infra Misconfiguration"
target_files: [../../security/fixtures/main.tf]
run:
  command: "kics scan -p security/fixtures --report-formats json --output-path kics-results"
expects:
  iac_report_generated: true
verified_in_session: false
verified_result: "blocked 2026-08-18: KICS ships no native Windows binary; requires Docker, and Docker Desktop's daemon is not running on this machine (client v29.6.2 present, npipe connection failed)"
