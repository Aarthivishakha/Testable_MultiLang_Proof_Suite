tool: KICS
slug: kics
language: javascript
category: security
technique: "IaC Static Analysis"
classification: "Infra Misconfiguration"
target_files: [../../security/fixtures/main.tf]
run:
  command: "kics scan -p security/fixtures --report-formats json --output-path kics-results"
expects:
  iac_report_generated: true
verified_in_session: false
verified_result: "valid Terraform fixture supplied; requires KICS"
