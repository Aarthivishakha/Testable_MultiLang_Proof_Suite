tool: Checkov
slug: checkov
language: javascript
category: security
technique: "IaC Static Analysis"
classification: "Infra Misconfiguration"
target_files: [../../security/fixtures/main.tf]
run:
  command: "checkov -d security/fixtures --output json"
expects:
  iac_report_generated: true
verified_in_session: true
verified_result: "ran 2026-08-18: called checkov.main.Checkov().run() directly with -o json against security/fixtures/ -- real JSON report: 3 passed, 2 failed checks (checkov_version 3.3.11); the earlier CLI-print issue was an invocation quirk (-o cli/--compact suppressed output), resolved by requesting -o json"
