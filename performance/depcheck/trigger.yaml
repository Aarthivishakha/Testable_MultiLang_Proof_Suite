tool: depcheck
slug: depcheck
language: javascript
category: performance
technique: "Dependency Analysis"
classification: "Unused/Bloat Dependency Detection"
target_files: [../../package.json, ../../src/calculator.js]
run:
  command: "npx depcheck --json"
expects:
  depcheck_report_generated: true
verified_in_session: true
verified_result: "ran 2026-08-18 with npx depcheck --json against repo: real output -- 3 unused devDependencies found (@stryker-mutator/core, @stryker-mutator/mocha-runner, jscpd)"
