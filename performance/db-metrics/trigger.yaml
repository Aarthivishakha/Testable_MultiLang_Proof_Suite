tool: DB Metrics (custom)
slug: db-metrics
language: javascript
category: performance
technique: "Database Performance Monitoring"
classification: "Query Latency / Connection Pool Health"
target_files: [../../performance/db-metrics/collect.mjs, ../../performance/db-metrics/sample-metrics.json]
run:
  command: "node performance/db-metrics/collect.mjs"
expects:
  db_metrics_collected: true
verified_in_session: false
verified_result: "deterministic fixture supplied; DB_METRICS_FILE can select real metrics"
